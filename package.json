{
  "name": "resume",
  "version": "1.0.0",
  "description": "A professional portfolio built with React.",
  "main": "src/index.js",
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build"
  },
  "keywords": [
    "portfolio",
    "react",
    "web",
    "development"
  ],
  "author": "Qaisar",
  "license": "MIT",
  "homepage": "https://QaisarRajput.github.io/resume",
  "dependencies": {
    "gh-pages": "^3.2.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "react-scripts": "4.0.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
